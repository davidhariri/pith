# Copy to ./config.yaml in the repo root (or set PITH_CONFIG to another path).
# Or run `uv run pith setup` to generate this interactively.
#
# Paths are derived from the config file location:
#   workspace: <config_dir>/workspace/
#   database:  <config_dir>/memory.db
#   logs:      <config_dir>/workspace/.pith/logs/

version: 1

model:
  provider: anthropic
  model: claude-sonnet-4-20250514
  api_key_env: ANTHROPIC_API_KEY
  base_url: null
  temperature: 0.2
